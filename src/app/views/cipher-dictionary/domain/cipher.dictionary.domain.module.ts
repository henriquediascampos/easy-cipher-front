import { CipherDictionaryService } from './service/cipher-dictionary.service';
import { NgModule } from '@angular/core';

@NgModule({
    providers: [CipherDictionaryService],
})
export class CipherDictionaryDomainModule { }
